[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "union-contract-system"
version = "1.0.0"
description = "Union Contract System"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
